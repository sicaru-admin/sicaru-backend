[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx medusa build"]

[start]
cmd = "npx medusa db:migrate && npx medusa start"
