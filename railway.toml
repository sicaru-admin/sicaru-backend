[build]
builder = "nixpacks"
buildCommand = "DISABLE_ADMIN=true npm run build"

[deploy]
startCommand = "DISABLE_ADMIN=true HOST=0.0.0.0 PORT=9000 npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 9000
