[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "NODE_OPTIONS=--max-old-space-size=2048 HOST=0.0.0.0 PORT=9000 npm run build && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 9000
