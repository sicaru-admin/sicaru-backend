[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "HOST=0.0.0.0 PORT=9000 npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 9000
