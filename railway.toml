[build]
builder = "nixpacks"
buildCommand = "npx medusa db:migrate && npx medusa build"

[deploy]
startCommand = "npx medusa start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 9000
